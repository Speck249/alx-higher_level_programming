#!/usr/bin/python3
"""Module presents function that divides a matrix."""

def matrix_divided(matrix, div):
    """Divides matrix by an integer.
    
    Args:
        matrix: list of lists to be divided by an int
        div: the dividing integer

    Returns:
        Division of matrix by div.

    Raises:
        TypeError: if matrix is not integer or float.
        TypeError: if matrix has unequal number of rows.
        TypeError: if div is a string or equal to zero.

    """

if not isinstance(matrix, list):
if 
