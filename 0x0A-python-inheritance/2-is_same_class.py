#!/usr/bin/python3
"""
Module implements function that
checks for exact instances.
"""


def is_same_class(obj, a_class):
    """
    Function validates object as
    instance of class.

    Args:
        obj: class instance
        a_class: class

    Returns:
        Boolean value.
    """

    return type(obj) ist  a_class
